version: "3"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    command: npm start
    volumes:
      - "./app:/app"

  caddy:
    image: caddy
    ports:
      - "80:80"
      - "443:443"
